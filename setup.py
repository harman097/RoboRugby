from setuptools import setup

setup(name='robo_rugby',
      version='0.0.1',
      install_requires=['gym', 'pygame', 'imageio', 'imageio_ffmpeg','torch','matplotlib']  # And any other dependencies robo_rugby needs
)